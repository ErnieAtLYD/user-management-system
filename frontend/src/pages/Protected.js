const Protected = () => {
  return (
    <main className='container'>
      <h1>Protected</h1>
      <p>
        This page is protected. You should only see this content if you are
        logged in.
      </p>
    </main>
  );
};

export default Protected;
